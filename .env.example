# Never commit real credentials. Configure Modal with `modal token set` or
# export these values only in your local shell/CI secret store.
MODAL_TOKEN_ID=
MODAL_TOKEN_SECRET=

# Optional local paths
TANTRA_DATA_ROOT=./ml/data
TANTRA_ARTIFACT_ROOT=./ml/artifacts
